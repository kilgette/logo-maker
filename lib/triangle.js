class Triangle {
  constructor(color) {
    this.color=color

  }
  render() {
    //Generate the SVG code for the triangle shape
    //Implement the logic here to return the SVG markup
    return this.color
  }
}

module.exports = Triangle;